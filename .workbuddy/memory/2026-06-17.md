## 2026-06-17

- 提交推送: chore: add cargo check to allowed permissions (eab86bd, fix/optimization-sweep-timeouts-panics)
- Docker 部署修复:
  - 新建 `.dockerignore` — 排除 target/、web/node_modules/、.git/ 等
  - 重写 `Dockerfile` — 三阶段构建 (frontend → Rust → runtime)，修复依赖缓存、加入 SvelteKit 前端构建、装 curl 做 health check
  - 新建 `deploy/docker-compose.yml` — 主应用编排，含 volumes、env vars、runner 注释模板
  - 更新 `deploy/README.md` — 补上主应用 Quick Start 章节
