# 2026-04-22 工作日志

## IronForge 项目启动
- 用户决定用 Rust 从零造一个 Git 托管平台（对标 Gitea/Forgejo 全功能）
- 项目代号: IronForge（铁匠铺）
- 完成架构方案设计（7 个 crate，详见 ARCHITECTURE.md）
- 完成 Phase 0 项目初始化：
  - Cargo workspace + 7 个 crate 全部创建
  - 技术选型：Axum 0.8 + russh + gix + SeaORM + clap
  - CLI 入口：serve / create-repo / generate-ssh-key / migrate
  - Git 协议层：pkt-line 编解码、upload-pack/receive-pack 框架
  - HTTP 服务：REST API（仓库 CRUD、用户注册、健康检查）
  - SSH 服务：stub（Phase 1 集成 russh）
  - 配置管理：TOML 配置文件，自动生成默认配置
- 编译通过，`ironforge create-repo testuser myproject` 测试成功
- 多个 Rust 生态 API 不稳定问题（gix/argon2/russh/axum），已用 git CLI fallback 解决

## Rust 环境配置
- 用户之前未安装 Rust，已通过 rustup 安装 1.95.0
- 用户要求：非 sudo 命令直接执行，无需确认
