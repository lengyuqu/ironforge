# 2026-06-12

## 项目整理 — 迁移文件和清理

### 操作
- 将顶层 `.workbuddy/memory/`（18 个工作日志）合并到 `ironforge/.workbuddy/memory/`
- 将 `.claude/`（settings.local.json）迁移到 `ironforge/.claude/`
  - ⚠️ 全局 gitignore 排除了 `**/.claude/settings.local.json`，已 force-add 强制追踪
- 将 `ironforge-docs/`（9 份分析文档）迁移到 `ironforge/ironforge-docs/`
- 创建 `ironforge/testdata/` 目录（含 `.gitkeep`），用于后续测试文件

### Git 提交流
- commit `011f6be` — "chore: 迁移 .claude / .workbuddy/memory / ironforge-docs 到项目内"
- 推送至 `github/main` ✅
- 推送至 `origin/main`（Gitea 私有库） ✅

### 后续（用户计划）
- 用户可安全删除 `/Users/yuqu/Desktop/帮我做个方案/` 文件夹
- 然后 `git clone` 即可恢复完整开发环境
- 注意：`data/ssh_host_key`（SSH 私钥）未提交，需单独备份
