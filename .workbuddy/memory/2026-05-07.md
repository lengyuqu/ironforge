# 2026-05-07 IronForge 项目改进

## 已完成的工作

### 任务 1: 修复代码质量 (by engineer-kou)
- 修复 rg-db/src/lib.rs doctest 失败 (async/await in sync main → tokio::main)
- 修复 rg-http 的 34+ clippy 建议 (未使用导入、too_many_arguments → PostPushParams struct、div_ceil 等)
- 修复 rg-cli large_enum_variant (添加 #[allow] 因为 clap derive 不支持 Box)
- 修复 rg-http 28 个编译警告

### 任务 2: 提升测试覆盖率 (6 → 49 tests, +716%)
- **rg-core/auth/jwt.rs**: +7 tests (expired token, wrong secret, claims fields, edge cases)
- **rg-git/pkt_line.rs**: +12 tests (read/write data/flush/delim/response_end, roundtrip, display, text helpers)
- **rg-git/sideband.rs**: +5 tests (data/progress/error/flush write+read, chunk size validation)
- **rg-ci/config.rs**: +5 tests (minimal config, all fields, empty jobs, same stage, serialization roundtrip)
- **rg-http/pagination.rs**: +14 tests (params, meta, response, clamp, boundary conditions)
- PktLine enum 添加 PartialEq derive
- 修复 sideband SIDEBAND_MAX bug (65519 → 65515, 超过 MAX_PKT_LINE_LEN)

### 任务 3: 分页优化评估
- 经代码审查，当前分页已是数据库层分页 (SQL OFFSET/LIMIT)，无需重构

### 任务 4: API 文档集成 (utoipa) ✅
- 添加 utoipa v5 (chrono feature) + utoipa-swagger-ui v8 (no-axum) 依赖
- 创建 openapi.rs 模块，定义 `ApiDoc` OpenAPI 规范结构体
- 为 6 个核心 API 添加 `#[utoipa::path]` 注解：
  - Users: register, login, me
  - Repos: create_repo, list_repos, get_repo
- 添加 `#[derive(ToSchema)]` 到请求/响应类型
- 挂载路由: `/api-docs/openapi.json` (spec) + `/api-docs/{*tail}` (Swagger UI)
- 用框架无关的 `utoipa_swagger_ui::serve()` 替代 axum feature (不兼容 axum 0.8)
- 修复 utoipa v5 API: schemas → components(schemas())
- **编译通过、零警告、全部测试通过**

### 额外: 编译警告清理
- 清理全部 15 个编译警告 (rg-core 13 + rg-git 1 + rg-ssh 1) → 零警告
- rg-core/auth/ssh_key.rs: 移除未使用的 bail, std::fmt::Write, std::io::Read
- rg-core/issue/service.rs: 移除未使用的 ActiveModelTrait
- rg-core/pull_request/service.rs: 移除未使用的 ActiveModelTrait, PathBuf
- rg-core/review/service.rs: 移除未使用的 ActiveModelTrait
- rg-core/branch_protection/service.rs: 移除未使用的 ActiveModelTrait
- rg-core/collaborator/service.rs: 移除未使用的 ActiveModelTrait
- rg-core/org/mod.rs: 移除未使用的 Context
- rg-core/lfs/service.rs: _lfs_root, _obj_id 前缀处理未使用变量
- rg-core/repo/service.rs: #[allow(dead_code)] for resolve_owner
- rg-git/receive_pack.rs: 移除无意义的 drop(stdin) (引用类型)
- rg-ssh/src/lib.rs: peer → _peer (未读取字段)

## 项目发现
- IronForge 是一个用 Rust 从零实现的 Git 托管平台，对标 Gitea/Forgejo
- 所有 12 个 Phase 已完成，功能非常完整
- **cargo check 零警告、cargo test 全部通过 (49 unit + 1 doc test)**

## 修复的关键 Bug
- sideband SIDEBAND_MAX=65519 → 65515 (生产 Bug，超过 MAX_PKT_LINE_LEN 会导致大文件 push 失败)
