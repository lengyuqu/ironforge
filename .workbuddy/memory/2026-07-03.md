# 2026-07-03 工作日志

## 代码更新
- 从 origin (司内 Git) 拉取 main 分支：`32e7792..126a299`，109 文件变更（+7,635/-1,118）
- 主要更新：22个新契约检查脚本、前端新页面（分支保护/协作者/镜像/导入/安全设置/Tokens）、FileEditor 组件、API 大面积更新、通知系统、i18n 扩展
- GitHub 远程已同步

## 项目审计（SoftwareCompany 团队）
- 完成前后端功能缺陷与设计矛盾全面审计
- 产出 `ironforge-docs/audit-report-2026-07-03.md`（20 个缺陷，含 2 Critical / 6 High）
- **Critical**：WebSocket 通知广播无用户隔离（信息泄露）+ validate_username 交叉校验缺失
- **High**：CORS permissive/CSP unsafe-inline（10 天未修复）、认证函数碎片化（4 种提取器）、std::sync::Mutex 在 async 使用、密码重置侧信道、i18n 键结构不一致（8 个键）
- 上次审计修复率仅 25%（2/8）
- 架构文档与代码多处偏差：PostgreSQL 声明、gix 100% 声称、middleware 目录、SSR 支持
- 团队前端审计后更新报告至 28 个缺陷（+8 前端专属发现）：
  - **新 High**：repo.private 翻译键缺失（首页显示原始键文本）、/help 路由缺失（Navbar 死链）、_base/client API 130 行重复代码
  - **新 Medium**：FileEditor 反斜杠转义错误、InstanceBanner $derived 语法错误、onMount 在 .svelte.ts 无效、authToken 重复声明
  - **新 Low**：AuthLoginResponse MFA 字段类型缺失
  - 新增 10 天内引入的债务跟踪表
- 团队后端审计后更新报告至 33 个缺陷（+5 后端深度发现）：
  - **新 High**：~55 处 API 错误响应返回原始 StatusCode 元组而非 AppError JSON（sso/mfa/audit/archive/packages）
  - **新 High**：AppError 变体风格不一致 — PascalCase vs snake_case 混用（repos/issues/pulls/releases/users）
  - **新 Medium**：不安全的原始 SQL — execute_unprepared() + format!()（repo/service.rs, code_indexer.rs）
  - **新 Medium**：事务覆盖几乎为零 — 仅 issue_label_ops.rs 使用事务，Issue/PR/Merge 均无保护
  - **新 Medium**：rg-http 直接依赖 rg-ci — 分层架构违规
  - 更新 unwrap() 清单至 15 处（含 RwLock ×7 锁污染风险）

## 文档对齐
- 修复 ARCHITECTURE.md 8 处文档与代码实际状态偏差：
  1. 架构图 PostgreSQL → SQLite（仅支持 SQLite）
  2. 数据库选型表移除 PostgreSQL 声明
  3. 工作区根目录名 rustgit/ → ironforge/
  4. rg-http 目录结构更新：routes/ + middleware/ → api/（扁平）+ git_v2/security/ws/oci
  5. rg-db models/ → entities/，migration/ → migrations/
  6. 配置路径 configs/default.toml → ironforge.toml + ironforge.example.toml
  7. Phase 0-5 checklist 全部标记完成 + 添加状态说明
  8. gix 进度 100% → ~70%（12 处 CLI fallback），SvelteKit SSR ✅ → ❌ SPA 模式
- 提交并推送 ARCHITECTURE.md 对齐 + 审计报告到两个远程仓库（a653b48）

## 功能测试指南
- 新增 `ironforge-docs/functional-test-guide-2026-07-03.md`：23 个测试用例（T01-T23）+ 15 项前端验证（F01-F15）
- 覆盖 P0 核心（健康检查/认证/PAT/仓库/Git HTTP+SSH）、P1 业务（文件浏览/Issues/PR/Wiki/分支保护/协作者/CI-CD/管理员）、P2 增强（Release/包注册表/通知/搜索/MFA/Swagger/WebSocket/组织）
- 每个用例含可执行 curl/git 命令、预期结果和通过标准
- 提交推送至两个远程仓库（5ab6a96）

## 缺陷批量修复（28/33 修复）
基于 `audit-report-2026-07-03.md` 全量修复，分两个 session 完成：

### Session 1 修复（13 项）
- C-1: WebSocket per-user channel 隔离
- C-2: register() 调用 validate_username()
- H-1: CORS 白名单 build_cors_layer() + IRONFORGE_CORS_ORIGINS
- H-2: CSP 文档化限制说明
- H-4: PermissionCache std::Mutex → tokio::RwLock
- H-6/H-7: i18n 8 个缺失键 + repo.private 翻译
- H-8: /help 页面创建
- H-9/M-11: _base/client API 去重 + authToken 统一
- M-8/M-9/M-10: FileEditor/InstanceBanner/onMount 前端修复
- M-12: 原始 SQL 参数化
- L-1: unwrap 消除（RwLock + sso）

### Session 2 修复（15 项）
- H-3: AuthUser FromRequestParts extractor 统一认证
- H-5: forgot_password 100ms 延迟归一化时序
- H-10: sso/mfa/audit/archive/packages 5 文件 → AppError
- H-11: 10 文件 100+ 处 PascalCase → snake_case 批量替换
- M-6: CI Job Token 死代码 TODO(phase-22) 文档
- M-13: create_issue SeaORM 事务包裹
- M-14: CiTrigger trait + DI — rg-http 不再直接依赖 rg-ci
  - TriggerPipelineParams/CiTrigger 移至 rg-core::ci
  - rg-ci 实现 CiEngine trait
  - HttpServerConfig 接收 Arc<dyn CiTrigger>
  - rg-cli 构造 CiEngine 注入
  - rg-http/Cargo.toml 移除 rg-ci 依赖
- L-3: 被 H-4 RwLock 修复覆盖
- L-4: audit.rs page_size→per_page + 1-based 分页; search.rs 统一默认值
  - 前端 client.svelte.ts + audit +page.svelte 同步更新

### 编译验证
- `cargo build --release` → 0 warnings, 0 errors ✅

### 未修复（5 项，留待下迭代）
- H-2 (CSP nonce 完全移除), L-2/L-5 (文档同步), L-6 (MFA 类型), M-1~M-5
