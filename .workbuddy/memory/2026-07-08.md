# 2026-07-08

## 提交本地 WIP：多数据库后端支持（SQLite/Postgres/MySQL）

用户要求"更新代码" → 发现 origin/main 与 github/main 均已同步（0 新提交），本地仅有未提交改动 → 用户选择"提交本地更改"。

按主题拆成 3 个 commit（本地 main 领先 origin/main 3 个提交，未推送）：
- `af9288a` feat(db): 多后端支持
  - rg-db/src/lib.rs: 新增 `DbBackend` 枚举、`detect_backend()`（按 scheme 推断）、`connect_with_pool()` 分发到 connect_sqlite/postgres/mysql
  - 新文件 crates/rg-core/src/search/dialect.rs: FTS5/tsvector/FULLTEXT 三后端方言 helper
  - search/{service,code_indexer}.rs、repo/service.rs 改用方言 helper；rg-cli/main.rs 备份命令非 SQLite 报错退出
  - Cargo.toml 开启 `sqlx-postgres`/`sqlx-mysql`；3 个 FTS5 迁移 + 4 个 ops 文件方言适配
- `39739aa` docs: ironforge-docs/db-multi-backend-design-2026-07.md + system-analysis-by-module-2026-07.md
- `44e4828` chore: .workbuddy 记忆笔记（2026-07-06 修改 + 2026-07-07 新增）

注意：未运行 cargo build 验证编译（sqlx 三后端重编耗时大）；提交是用户自己的 WIP，编译正确性待用户自行验证。

## 推送
用户要求"推送" → 已将两个远端都推送：
- `git push origin main`：成功 ebb6728..44e4828（git233 内部）
- `git push github main`：成功 ebb6728..44e4828（公开镜像）。有 `fatal: failed to store: 100001` 凭据缓存警告，但不影响推送结果。
- 当前本地 main 与 origin/main、github/main 全部同步（领先 0，干净）。

## 文档整理合并（ironforge-docs 重构）

用户要求"进行文档整理合并"，方向确认：全部含根目录入口 · 结构重构+去重归档 · 开发者+AI 兼顾。

完成动作：
- ironforge-docs/ 下新建 architecture/ analysis/ comparison/ ci/ testing/ 子目录，按主题归位 19 份报告
- 过程文档（analysis-plan/notes 约 3348 行、remediation-plan）与根目录散落文件（frontend-layout-audit/plan/defect-*）移入 archive/
- AGENTS.md 与 CLAUDE.md 整文件重复（仅"Codex/Claude Code"命名不同，48 行 diff），AGENTS.md 改为指向 CLAUDE.md 的兼容性指针
- 两份重叠改进报告（2026-06-09 全方位改进 + 2026-06-17 源码优化）融合为 analysis/improvement-analysis.md，原两份归档
- 重写 ironforge-docs/README.md 为新索引（单一事实来源）
- 更新 AGENT.md / CLAUDE.md / README.md 中对 ironforge-docs/ 的旧路径引用
- docs/project-audit-2026-06.md 移入 testing/（与 audit-report-2026-07-03 并列，保留演进时间线）

注意：以上为文件移动（普通 mv，未 git commit），用户可自行提交。内容时效需后续对照代码核对（gix 进度、包注册表数量等以 CLAUDE.md 为准）。
