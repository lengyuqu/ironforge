# 2026-06-02 工作日志

## 代码注释补充

- 为 `rg-ssh/src/lib.rs` 补充了 SSH 流关闭顺序的详细注释（对应踩坑经验 #3）
  - 在 `exec_request` 函数中添加了关于 exit_status → shutdown → drop 正确顺序的注释
  - 说明了为什么这个顺序对正确通知客户端退出码至关重要

- 为 `rg-git/src/protocol/v2.rs` 中的 `list_refs` 函数补充了关于 `for-each-ref` 不列出 HEAD 的注释（对应踩坑经验 #5）
  - 说明了 `git for-each-ref` 命令的默认行为（不列出 HEAD）
  - 解释了为什么使用 gix API (`repo.references().all()`) 更可靠
  - 描述了如何正确处理 symbolic refs (like HEAD)

- 为 `rg-http/src/lib.rs` 中的 `handle_info_refs` 函数补充了关于 HTTP Git 协议 Content-Type 的注释（对应踩坑经验 #6）
  - 详细列出了 Git Smart HTTP 协议所需的各种 Content-Type
  - 说明了错误的 Content-Type 会导致 git 客户端静默失败
  - 强调了必须严格按照 Git Smart HTTP 规范设置 Content-Type

## 文档更新

- 更新了 `CLAUDE.md` 的常见错误排查部分，添加了两个新的踩坑经验：
  1. `HEAD` not found in ref list | `git for-each-ref` 不列出 HEAD | 用 gix API (`repo.references().all()`) 替代
  2. `fatal: not a valid ref` (HTTP clone) | Content-Type 不正确 | 确保 `info/refs` 响应使用正确的 Content-Type

## 任务状态

所有 5 个任务已完成：
1. ✅ 为 rg-ssh/src/lib.rs 补充 SSH 流关闭顺序注释
2. ✅ 为 rg-core/src/repo/service.rs 补充 HEAD 引用处理注释（实际在 rg-git/src/protocol/v2.rs 中完成）
3. ✅ 为 rg-http/src/lib.rs 补充 HTTP Git 协议 Content-Type 注释
4. ✅ 为数据库迁移文件补充文档注释（发现已有模块级文档，任务已完成）
5. ✅ 更新 ARCHITECTURE.md 和 CLAUDE.md 文档

