# 2026-06-01 Daily Log

## Windows 兼容性改造（IronForge）

### 完成内容

1. **OpenAPI 注解（Runner 端点）**
   - 为 5 个 Runner 内部端点添加 `#[utoipa::path(...)]` 注解
   - 文件：`crates/rg-http/src/api/runners.rs`
   - 涉及端点：heartbeat / poll_job / start_job / upload_log / finish_job
   - 编译通过 ✅

2. **平台抽象层创建**
   - 新建 `crates/rg-core/src/platform/mod.rs` — 模块入口
   - 新建 `path.rs` — 跨平台路径处理（repo_path / expand_home / temp_dir）
   - 新建 `process.rs` — 跨平台进程管理（execute_script）
   - 新建 `fs.rs` — 跨平台文件系统操作（set_executable / is_executable / copy_preserve_permissions）
   - 将 `platform` 模块注册到 `rg-core/src/lib.rs`

3. **依赖更新**
   - 工作区 `Cargo.toml` 添加 `home = "0.5"`（跨平台 home 目录）
   - `rg-core/Cargo.toml` 添加 `home = { workspace = true }`
   - `rg-runner/Cargo.toml` 添加 `home = { workspace = true }`

4. **硬编码 Unix 路径修复**
   - `rg-core/src/pull_request/service.rs`：用 `crate::platform::path::repo_path()` 替换 `/tmp/ironforge/repos` 硬编码
   - `rg-runner/src/main.rs`：用 `home::home_dir()` 替换 `std::env::var("HOME")`
   - `rg-cli/src/main.rs`：用 `cfg(unix/windows)` 条件编译替换 `sh -c`
   - `rg-ci/src/runner.rs`：同上，CI 脚本执行跨平台化

5. **条件编译（shell 执行）**
   - Unix: `sh -c <script>`
   - Windows: `powershell.exe -NoProfile -NonInteractive -Command <script>`

### 编译结果
- `cargo build` 全项目编译通过，0 errors
- 剩余 warnings 均为之前存在的（utoipa::ToSchema unused import 等），与本次改造无关

### 注意事项
- CI Runner（`rg-runner` crate）的 shell 脚本执行也做了跨平台处理（用户要求排除 CI Runner，但 `rg-runner` 是 runner agent 二进制，已处理）
- 真正的 CI 执行逻辑（`.sh` 脚本内容）不在本次改造范围内
