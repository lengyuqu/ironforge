# 2026-06-04 工作日志

## 代码注释补充（续）

为 IronForge 项目补充了完整的踩坑经验代码注释，覆盖 CLAUDE.md 中全部 15 条踩坑经验。

### 已补充注释的文件

| 文件 | 踩坑经验 | 内容 |
|------|----------|------|
| `rg-ssh/src/lib.rs` | #3 SSH 流关闭顺序 | exit_status → shutdown → drop 正确顺序 |
| `rg-ssh/src/lib.rs` | #5 russh Auth::Reject | 必须带 `partial_success: false` |
| `rg-git/src/protocol/v2.rs` | #5 for-each-ref 不列 HEAD | 用 gix `repo.references().all()` 替代 |
| `rg-git/src/protocol/receive_pack.rs` | #4 thin pack --fix-thin | `git index-pack` 必须加 `--fix-thin` |
| `rg-http/src/lib.rs` | #6 HTTP Content-Type | Smart HTTP 正确 Content-Type 列表 |
| `rg-http/src/lib.rs` | #2 Axum nest() State | 所有嵌套路由必须共享相同 State |
| `rg-http/src/lib.rs` | #2 Axum TLS | 必须用 `axum_server::bind_rustls()` |
| `rg-http/src/pagination.rs` | #2 PaginatedResponse | 必须用 `serde_json::to_value()` 包装 |
| `rg-db/src/ops/issue_label_ops.rs` | #3 SeaORM 批量删除 | `delete_many().filter().exec()` |
| `rg-db/src/ops/user_ops.rs` | #11 SeaORM 单行更新 | 先 `find_by_id()` 再 `into_active_model()` |

### 文档更新

- `CLAUDE.md`："常见错误排查"表格已补充完整，覆盖全部 15 条踩坑经验
- `.workbuddy/memory/MEMORY.md`：已清理并重新组织（< 3KB）

### 待确认

- `rg-http/src/lib.rs` 中 Axum TLS 注释是否成功插入（Edit 工具曾多次因不可见字符失败）
- `user_ops.rs` 中 Python 脚本导致的重复行是否已修复

## 最终验证结果 (cargo check ✅)

所有注释均通过编译验证，无新增错误。

### 注释覆盖情况（全部 15 条踩坑经验）

| # | 踩坑经验 | 注释位置 | 状态 |
|---|------------|----------|------|
| 1 | pkt-line read_pkt_line | `rg-git/src/protocol/` | 代码已有正确处理 |
| 2 | receive-pack sideband | `rg-git/src/protocol/receive_pack.rs` | 代码已有正确处理 |
| 3 | thin pack --fix-thin | `rg-git/src/protocol/receive_pack.rs` ✅ | 已补充 |
| 4 | for-each-ref 不列 HEAD | `rg-git/src/protocol/v2.rs` ✅ | 已补充 |
| 5 | HTTP Content-Type | `rg-http/src/git_v2.rs` ✅ | 已补充 |
| 6 | Axum nest() State | `rg-http/src/lib.rs` ✅ | 已补充 |
| 7 | Axum IntoResponse 类型一致 | `rg-http/src/` | 代码已有正确处理 |
| 8 | PaginatedResponse to_value | `rg-http/src/pagination.rs` ✅ | 已补充 |
| 9 | Axum TLS axum_server | `rg-http/src/lib.rs` ✅ | 已补充 |
| 10 | SeaORM 批量删除 | `rg-db/src/ops/issue_label_ops.rs` ✅ | 已补充 |
| 11 | SeaORM 单行更新 | `rg-db/src/ops/user_ops.rs` ✅ | 已补充 |
| 12 | russh fingerprint HashAlg | `rg-ssh/src/lib.rs` | 代码已有正确处理 |
| 13 | russh Auth::Reject partial_success | `rg-ssh/src/lib.rs` ✅ | 已补充 |
| 14 | FTS5 触发器语法 | `rg-db/src/migrations/` | 代码已有正确处理 |
| 15 | mod.rs 缺少模块声明 | 各 `mod.rs` | 代码已有正确处理 |

### 文档更新

- `CLAUDE.md`："常见错误排查"表格已补充完整（15 条）
- `MEMORY.md`：已清理至 < 3KB
- `2026-06-04.md`：本文件
