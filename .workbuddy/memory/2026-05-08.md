# 2026-05-08

## IronForge 推送至 GitHub
- 确认 `gh` CLI 已登录账号 `lengyuqu`（keyring 认证，权限 repo/workflow/gist/read:org）
- 通过 `gh repo create ironforge --public` 创建 GitHub 仓库
- 仓库地址: https://github.com/lengyuqu/ironforge
- 添加 `github` remote，`git push github main` 成功推送
- 原有 `origin` remote（git233.phynews.com）保持不变
