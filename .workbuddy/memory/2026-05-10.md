# IronForge Daily Log

## 2026-05-10

### 完成内容：gix 迁移进度检查与报告生成

**背景**：
- 用户要求检查 IronForge 项目中 git CLI → gix 的迁移进度
- 区分已迁移和未迁移的部分

**分析方法**：
1. 使用 `grep` 扫描所有 `Command::new("git")` 调用（排除 target/）
2. 使用 `grep` 扫描所有 `gix::` API 使用
3. 逐文件检查迁移状态

**检查结果**：

| 指标 | 数值 |
|------|------|
| gix API 调用次数 | **32 处** |
| git CLI 调用次数 | **18 处** |
| 使用 gix 的文件数 | **9 个** |
| 仍使用 CLI 的文件数 | **7 个** |
| **整体迁移进度** | **约 50%** |

**已迁移部分**（使用 gix）：
- ✅ **rg-http crate**: 6 处（读取 commit/tree/文件内容）
- ✅ **rg-git crate**: 8 处（引用操作、pack 协议）
- ✅ **rg-core crate**: 2 处（`get_head_sha()` 函数）
- ✅ **rg-cli crate**: 1 处（验证仓库路径）

**未迁移部分**（仍使用 git CLI）：
- ❌ **rg-http**: 2 处（GPG 签名验证 - gix 不支持）
- ❌ **rg-ci**: 2 处（CI 配置读取 - **可迁移**，已有 gix 实现）
- ❌ **rg-git**: 3 处（pack-objects, index-pack - gix 不支持）
- ❌ **rg-core**: 11 处（PR merge/rebase/diff - 部分可迁移）

**关键发现**：
1. ✅ 项目**已经开始迁移**，不是从零开始
2. ⚠️ `rg-ci/src/lib.rs` 可能**有重复代码**（CLI 版本 + gix 版本）
3. ❌ GPG、Pack、Rebase 等功能 **gix 不支持**，无法迁移
4. ✅ 有成功的迁移案例（`get_head_sha()`），可作为参考

**输出文档**：
1. 📄 `/Users/yuqu/Desktop/帮我做个方案/ironforge-docs/gix-migration-status-report.md`
   - 详细的迁移进度报告（约 350 行）
   - 包含：已迁移部分、未迁移部分、按 crate 统计、按功能分类
   - 推荐行动计划（阶段 1/2/3）

2. 📄 `/Users/yuqu/Desktop/帮我做个方案/ironforge-docs/gix-migration-feasibility-analysis.md`（昨日生成）
   - 可行性分析报告
   - 包含：调用现状、gix 能力评估、迁移方案设计

**用户决策**：
- 选择 **选项 C：仅保存报告**
- 不立即启动迁移工作
- 将报告作为技术参考文档保存

**下一步**（未来可能）：
- 启动阶段 1：迁移低难度功能（6 处 CLI 调用，1-2 天）
- 或启动完整团队流程（4-5 周完成所有可迁移功能）
- 或等待 gix 库更成熟后再迁移

---

## 技术要点

### 检查脚本
```bash
# 检查 gix 使用情况
grep -rn "gix::" --include="*.rs" . | grep -v "target/" | wc -l

# 检查 git CLI 使用情况
grep -rn "Command::new(\"git\")" --include="*.rs" . | grep -v "target/"

# 列出使用 gix 的文件
grep -rl "gix::" --include="*.rs" . | grep -v "target/"
```

### 迁移进度按 Crate 统计
| Crate | 已迁移 | 未迁移 | 进度 |
|-------|--------|--------|------|
| rg-http | 6 | 2 | 75% |
| rg-ci | 1 | 2 | 33% |
| rg-git | 8 | 3 | 73% |
| rg-core | 2 | 11 | 15% |
| rg-cli | 1 | 0 | 100% |

### 可立即迁移的功能（低垂果实）
1. CI 配置读取（rg-ci/src/lib.rs:150,167）- 2 小时
2. Commit 创建（rg-core/pull_request/service.rs:398）- 2 小时
3. Checkout（rg-core/pull_request/service.rs:419,456）- 4 小时
4. Fast-forward（rg-core/pull_request/service.rs:467）- 2 小时

**预计减少 CLI 调用**: 6 处  
**预计工作时间**: 1-2 天  

---

## 待确认事项

1. ⚠️ `rg-ci` crate 是否有重复实现？（CLI 版本 + gix 版本）
2. 是否需要在未来启动迁移工作？
3. 是否需要关注 gix 项目进展（https://github.com/Byron/gitoxide）？

---

---

## 2026-05-10 (续) — Gitea 1.26 功能差距分析

### 完成内容：生成 Gitea vs IronForge 功能差距报告

**背景**：
- 用户要求检查当前系统距离 Gitea 的功能差距并进行后续实施
- 按照方案 A：先分析，生成报告后等待用户切换到 Craft 模式实施

**分析方法**：
1. 使用 `WebFetch` 获取 Gitea 1.26 完整功能列表
2. 读取 `ARCHITECTURE.md` 了解 IronForge 架构设计
3. 读取 `.workbuddy/memory/2026-05-09.md` 了解已实现功能
4. 生成功能差距矩阵

**Gitea 1.26 关键新功能**（2026-04-18 发布）：
- ✅ Subpath 归档下载
- ✅ OpenAPI 渲染
- ✅ Terraform 状态注册表
- ✅ Actions Concurrency 控制
- ✅ Re-run 失败 Jobs
- ✅ User 徽章
- ✅ Vite 构建工具迁移
- ✅ CodeMirror 替换 Monaco

**IronForge 完成度评估**：

| 类别 | 完成度 | 评估 |
|------|--------|------|
| 核心 Git 托管 | 70% | ✅ 基本可用 |
| Issue/PR 管理 | 60% | ⚠️ 需要完善 |
| **CI/CD** | **5%** | ❌ **最大缺口** |
| **包注册表** | **0%** | ❌ **完全缺失** |
| Wiki | 20% | ❌ 需要完善 |
| 用户/组织管理 | 70% | ✅ 基本完成 |
| 搜索 | 80% | ✅ 基本完成 |
| Webhooks | 80% | ✅ 基本完成 |

**总体完成度**：**40-50%**

**按优先级分类的功能缺口**：

| 优先级 | 功能数 | 预计工作量 | 状态 |
|--------|--------|------------|------|
| **P0 (核心)** | 3 个 | 6-9 周 | ❌ 未实现 |
| **P1 (重要)** | 5 个 | 7-10 周 | ⚠️ 部分实现 |
| **P2 (有用)** | 5 个 | 9-13 周 | ❌ 未实现 |
| **P3 (锦上添花)** | 3 个 | 4-6 周 | ❌ 未实现 |

**P0 核心功能（必须实现）**：
1. ❌ CI/CD Runner 调度系统 - 0% 完成
2. ❌ CI/CD Job 执行引擎 - 0% 完成
3. ❌ CI/CD Artifact 管理 - 0% 完成

**输出文档**：
📄 `/Users/yuqu/Desktop/帮我做个方案/ironforge-docs/gitea-feature-gap-analysis.md`
- 完整的功能差距矩阵（10 个类别）
- 按优先级分类（P0/P1/P2/P3）
- 实施路线图建议（3 个阶段）
- 风险评估与缓解措施

**用户决策**：
- 选择 **方案 A：先分析，后实施**
- 分析已完成 ✅
- **需要切换到 Craft 模式**才能启动团队流程进行实施

**下一步**（需要 Craft 模式）：
1. 启动完整团队流程（产品经理 → 架构师 → 工程师 → QA）
2. 优先实施 P0 核心功能（CI/CD）
3. 预计 6-9 周完成 P0

---

## 收尾总结 (2026-05-10 12:06)

### 完成内容
✅ **gix 迁移分析** (2 份文档)
  - gix-migration-feasibility-analysis.md (可行性分析)
  - gix-migration-status-report.md (迁移进度)

✅ **Gitea 功能差距分析** (1 份文档)
  - gitea-feature-gap-analysis.md (功能对比)

✅ **文档索引创建**
  - ironforge-docs/README.md (文档导航)

### 核心发现
1. **gix 迁移**: 50% 完成，剩余 18 处 CLI 调用 (部分不可迁移)
2. **Gitea 功能**: 40-50% 完成，P0 (CI/CD) 为最大缺口
3. **项目实施**: 用户选择先分析、后实施，等待 Craft 模式

### 用户决策记录
- gix 迁移: 选项 C (仅保存报告)
- Gitea 差距: 选项 A (先分析，后实施)

### 下一步 (待用户决策)
- 切换到 Craft 模式启动团队流程
- 或继续完善文档
- 或开始实施 P0 功能 (CI/CD)

---

## 2026-05-10 (续) — Task #12: Runner 调度系统后端

### 完成内容：Runner 调度系统后端实现（进行中）

**背景**：
- 实现 Task #12: Runner 调度系统后端
- 包括：runner 注册、心跳、任务分发、长轮询支持

**已完成**：

1. ✅ **数据库层 (rg-db)**
   - 修复 `generate_token()` 使用 uuid 替代 rand
   - 添加 `runner_id: Option<i64>` 字段到 `pipeline_job::Model`
   - 实现 `find_pending_job()` - 查找待分配任务
   - 实现 `assign_job()` - 分配任务给 runner

2. ✅ **HTTP API 层 (rg-http)**
   - 修复结构体可见性（pub）- `PollJobResponse`, `RunnerInfoResponse`, `FinishJobResponse`
   - 移除未使用的 import
   - `rg-db` crate 编译成功 ✅
   - `rg-http` crate 编译成功 ✅（有未使用变量警告）

**进行中**：

1. 🔄 **`poll_job()` 实现**
   - 需要添加 Query 参数支持（timeout）
   - 需要实现长轮询逻辑
   - 当前返回 204 No Content（空实现）

2. ⏳ **等待 rg-cli 编译**
   - 数据库迁移需要 rg-cli 二进制
   - 已在后台启动编译（Task ID: 1CdwUw）

**待实现**：
- `start_job()` - Runner 开始执行任务
- `upload_log()` - 上传任务日志
- `finish_job()` - 完成任务

**下一步**：
1. 实现 `poll_job()` 长轮询逻辑
2. 运行数据库迁移（等待 rg-cli 编译完成）
3. 实现剩余 API handlers

---

_记录时间: 2026-05-10 12:00 GMT+8 (更新: 12:52)_

---

## 2026-05-10 (续) 13:13~14:00 — Runner 调度系统后端完成

### 完成内容：Runner 调度 API 全部实现并编译通过

**背景**：用户要求"都开始进行"并"继续并进行一次编译"，推进 Task #12。

**代码修改**：

1. ✅ **`rg-http/src/api/runners.rs`**：
   - 添加 `Query` 提取 `PollJobQuery.timeout` 参数
   - 实现 `poll_job()` 长轮询：用 `tokio::time::timeout` 包装轮询循环，每 3s 检查待分配任务，找到后 assign 并返回 `PollJobResponse`，超时返回 204
   - 实现 `start_job()`：校验 runner 归属 → 更新 job status=`running` + started_at → 更新 runner status=`busy`
   - 实现 `upload_log()`：校验 runner 归属 → 追加日志（existing + new）→ 更新 job log
   - 实现 `finish_job()`：校验 runner 归属 → 更新 job status/exit_code/finished_at → 更新 runner status=`online`
   - 添加 `get_stage_by_id()` 到 `pipeline_ops.rs`（供 `poll_job` 获取 pipeline_id）
   - 修复 `update_job_result()`：仅当 `log.is_some()` 时才更新 log 字段（避免覆盖已有日志）

2. ✅ **编译结果**：
   - `rg-http`：`Finished` ✅，0 warning
   - `rg-db`：`Finished` ✅，3 个 warning（迁移文件，良性）

**Bug 修复记录**：
- `finish_job` 中 `req.message` 误当 `log` 传入 → 移除 `message` 字段（serde 默认忽略未知字段，不影响 API 兼容）
- `update_job_result` 无条件 `Set(log)` 会覆盖日志 → 改为仅 `log.is_some()` 时更新
- 移除未使用的 import `std::time::Duration`

**任务状态**：Task #1 (Task #12) → `completed` ✅

---

## 2026-05-10 (续) 20:27 — 项目进度检查 + MEMORY.md 文档阅读指南

### 完成内容
1. 通读所有项目文档，检查当前项目进度
2. 精简 MEMORY.md（从 218 行 → ~100 行，去重合并）
3. 在 MEMORY.md 顶部新增「文档阅读指南」— 告诉 AI 助手（Claude Code/Codex/WorkBuddy）针对不同任务应读取哪些文件

### 文档阅读指南结构
- **必读层**：CLAUDE.md + ARCHITECTURE.md（所有任务都需读取）
- **按任务选读**：Git 协议、功能规划、gix 迁移、CI/CD Runner、P0/P1 开发、前端、数据库 — 各自映射到具体文档路径
- **文件路径速查**：所有文档的相对路径一览

---

## 2026-05-10 (续) 20:33~21:00 — Phase 17: CI/CD Runner 收尾（全部完成）

### 完成内容：Phase 17 全部 7 个 Step 实现 + 编译通过

**Step 1: Runner Token 认证中间件**
- `rg-http/src/api/runners.rs` 新增 `authenticate_runner` 中间件函数
- heartbeat/poll/start/log/finish 端点添加 `from_fn_with_state` 路由层中间件
- Bearer token 认证 + 自动更新 heartbeat
- register 端点不需要 token

**Step 2: 外部 Runner 模式（解决 embedded/external 冲突）**
- `rg-ci/src/lib.rs`: `trigger_pipeline()` 新增 `external_runners: bool` 参数
- 当 `external_runners=true` 时跳过 spawn embedded PipelineRunner，job 保持 pending
- `AppState` / `HttpServerConfig` / `PostPushParams` 新增 `external_runners` 字段
- `rg-cli`: 新增 `--external-runners` CLI flag + TOML 配置 `ci.external_runners`
- webhook push 触发、手动触发、重试触发均已传递参数

**Step 3: Runner Agent 二进制（rg-runner）**
- 新 crate `crates/rg-runner/`（bin: `ironforge-runner`）
- 功能：register / run 两个子命令
- 配置文件 `~/.ironforge/runner.toml` 自动保存
- 30s 心跳后台任务 + Long Polling（30s timeout）+ sh/Docker 执行
- reqwest HTTP client + clap CLI

**Step 4: Artifact 管理**
- DB 迁移 `m20260510_000004_create_artifacts`: artifacts 表（id, job_id, name, file_path, size, created_at, expires_at）
- Entity `rg-db/src/entities/artifact.rs`
- Ops `rg-db/src/ops/artifact_ops.rs`: create/list_by_job/list_by_pipeline/get/delete/delete_expired
- API `rg-http/src/api/artifacts.rs`: upload（需认证）/ list by pipeline / get / delete

**Step 5: Job 日志 WebSocket 实时推送**
- `ws.rs` 新增 `push_job_log()` 函数 + `ws_job_log_handler` 端点
- `GET /api/v1/ws/job/:job_id` — 按 job_id 过滤日志事件
- `upload_log()` 时 broadcast 到 WebSocket hub
- 已有 `ws_notifications_handler` 也转发 `job_log` 事件

**Step 6: Admin Runner 管理前端**
- 新页面 `web/src/routes/admin/runners/+page.svelte`
- 列表：ID/Name/Status Badge/Labels/Version/OS+Arch/Last Seen + Delete 按钮
- 删除确认模态框
- Admin dashboard 新增 Runners 卡片入口
- API client 新增 `admin.listRunners()` / `admin.deleteRunner()`
- 后端新增 `DELETE /api/v1/admin/runners/:id` + `runner_ops::delete_runner()`

**预存问题修复**
- `rg-cli/Cargo.toml` 添加缺失的 `serde_json` 依赖
- `rg-cli/src/main.rs` 实现 `run_job_local()` / `run_job_docker()` 函数（之前只有调用没有实现）
- 修复 `serde_json::Value::Array` unwrap_or 类型不匹配

**编译结果**: `cargo build` ✅ (0 errors, 2 warnings — unused imports in rg-cli)

---

## 2026-05-10 (续) 21:34~22:00 — Phase 18: gix 迁移（低难度替换）

### 完成内容：Phase 18 gix 迁移 — 5 处 git CLI → gix

**迁移清单**：

| # | 文件 | 功能 | 迁移方式 |
|---|------|------|----------|
| 1 | `rg-ci/src/lib.rs` — `read_ci_config()` | `git show sha:.ironforge-ci.yml` → gix `rev_parse_single` + blob | gix API |
| 2 | `rg-ci/src/lib.rs` — `has_ci_config()` | `git cat-file -e` → gix `rev_parse_single().is_ok()` | gix API |
| 3 | `rg-core/pull_request/service.rs` — `do_rebase_merge()` checkout ×2 | `git checkout <branch>` → gix `edit_reference(RefEdit)` with `Target::Symbolic` | gix API |
| 4 | `rg-core/pull_request/service.rs` — `do_rebase_merge()` ff ×1 | `git merge --ff-only` → gix `repo.reference()` | gix API |

**新增辅助函数**：
- `gix_set_head_to_branch(repo_path, branch)` — 通过 gix `edit_reference` 设置 HEAD 符号引用
- `gix_fast_forward(repo_path, base, head)` — 通过 gix `repo.reference()` 更新分支引用

**依赖变更**：
- `rg-ci/Cargo.toml`: 添加 `gix = { workspace = true }` 依赖

**未迁移（保留 CLI）**：
- `do_squash_merge` 的 `git merge --squash` + `git commit`（index 耦合，需整个流程一起替换）
- `do_rebase_merge` 的 `git rebase`（gix 不支持）
- `do_merge_commit` 的 `git merge --no-ff`（gix merge API 需额外开发）
- GPG 验证 ×2（gix 不支持）
- Pack 操作 ×3（gix 不支持）
- Diff ×2（需自行实现 numstat）
- Bare clone ×1（gix 不支持）

**迁移统计**：
| 指标 | 迁移前 | 迁移后 |
|------|--------|--------|
| git CLI 调用 | 18 处 | 13 处 |
| rg-ci CLI 调用 | 2 | **0** ✅ |
| 整体 gix 迁移进度 | ~50% | **~60%** |

**编译结果**: `cargo build` ✅ (0 errors, 3 warnings — 预存的 rg-runner + rg-cli warnings)
